{
  "name": "nflanalysis",
  "description": "A command line utility for analyzing nfl stats",
  "require": {
    "symfony/console": "4.2.2",
    "guzzlehttp/guzzle": "6.3.3",
    "vlucas/phpdotenv": "3.1.1"
  },
  "autoload": {
    "psr-4": {
      "Mox\\": "src/"
    }
  },
  "scripts": {
    "post-package-install": [
      "php -r \"file_exists('.env') || copy('env.example', '.env');\""
    ]
  },
  "bin": [
    "nflanalysis"
    ]
}

